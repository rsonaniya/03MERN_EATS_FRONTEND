import hero from "../assets/hero.png";
const Hero = () => {
  return (
    <div className="">
      <img src={hero} className="w-full max-h-[500px] object-cover" />
    </div>
  );
};

export default Hero;
